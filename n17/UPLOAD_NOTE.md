# Upload note

This is a self-contained folder for the solved `17×17` case.

Recommended repository placement:

```text
n17/
```

To verify after upload or after downloading the repository locally:

```bash
cd n17
python verify_n17.py
```

Expected result:

```text
D_mono(17, even) = D_mono(17, odd) = D_mono(17) = 26
```

Suggested commit message:

```text
Add exact verified result for monochromatic 17x17 case
```
