# START HERE — память и рабочая инструкция проекта

Последнее существенное обновление: **17 июля 2026 года**, после подготовки полного пятичасового разбиения по парам главной диагонали.

Этот файл — главная долговременная память проекта между веб-чатами. В начале нового чата его нужно прочитать **один раз** до анализа, изменений программ или запуска GitHub Actions. Если старый чат расходится с проверяемыми файлами репозитория, доверять репозиторию.

---

## 1. Задача

На доске `n × n` выбираются точки одного шахматного цвета (`x+y ≡ 0` или `1 mod 2`). Нужно максимизировать их число при условии, что никакие три выбранные точки не лежат на одной прямой любого наклона. Обозначение максимума — `D_mono(n)`.

Проверять нужно **все прямые**, а не только строки, столбцы и диагонали `±1`.

---

## 2. Обязательная структура репозитория

Каждый размер имеет отдельную папку непосредственно в корне:

```text
17x17/
18x18/
19x19/
20x20/
21x21/
22x22/
23x23/
...
```

Не использовать `N17/`, `n22/`, `board-23/` и не смешивать разные размеры в одной папке.

В папке размера хранятся:

- `README.md` со статусом;
- лучшие конфигурации;
- независимый проверяющий скрипт;
- программы поиска и верхней границы;
- сертификаты и итоговые JSON;
- заметки о следующем шаге;
- `runs/YYYY-MM-DD-run-RUN_ID/` для каждого значимого запуска.

Общие файлы (`README.md`, `START_HERE.md`, `LICENSE`, `.github/`) остаются в корне. Workflow-файлы находятся в `.github/workflows/`, но результаты каждого запуска архивируются внутри папки соответствующей доски.

---

## 3. Подтверждённый математический статус

| Размер | Результат | Статус | Папка |
|---:|---:|---|---|
| `17×17` | `D_mono(17) = 26` | закрыт | `17x17/` |
| `18×18` | `D_mono(18) = 27` | закрыт | `18x18/` |
| `19×19` | `D_mono(19) = 29` | закрыт | `19x19/` |
| `20×20` | `D_mono(20) = 30` | закрыт | `20x20/` |
| `21×21` | `D_mono(21) = 32` | закрыт | `21x21/` |
| `22×22` | `33 ≤ D_mono(22) ≤ 34` | активный фронтир | `22x22/` |

Размер закрыт только при наличии обеих частей:

1. независимо проверенной конфигурации нижней границы;
2. проверяемого доказательства верхней границы — сертификата или полного точного перебора.

Долгое отсутствие найденной конфигурации не является доказательством.

---

## 4. Активная задача: 22×22

В `22x22/config_33.json` есть проверенная конфигурация из 33 точек:

```bash
python 22x22/verify_22x22.py
```

Она доказывает `D_mono(22) ≥ 33`. Активная цель — либо найти 34 точки, либо строго доказать их невозможность.

Для чётной доски две чётности равносильны отражением `(x,y) -> (21-x,y)`, поэтому достаточно одной. При цели 34:

- в каждой строке и столбце не более двух точек;
- число строк с двумя точками — от 12 до 17;
- число столбцов с двумя точками — от 12 до 17;
- транспонирование позволяет оставить только `row_twos ≤ column_twos`.

Полное исходное разбиение содержит 21 пару:

```text
12 ≤ row_twos ≤ column_twos ≤ 17
```

Любая точная модель обязана содержать ограничения для всех максимальных прямых, включающих не менее трёх разрешённых точек.

---

## 5. Важные прогоны 22×22

### 29533939978 — техническая ошибка seed

В `random_seed` передали число вне диапазона знакового 32-битного целого. Обязательная защита:

```python
effective_seed = requested_seed % 2_147_483_647
```

Этот запуск почти не выполнил математического поиска.

### 29534667942 — длинная исходная модель

Архив: `22x22/runs/2026-07-17-run-29534667942/`.

- проверка 33 точек прошла;
- сохранены 20 из 21 раздела;
- все 20 получили `UNKNOWN`;
- 34 точки не найдены;
- ни один раздел не получил `INFEASIBLE`;
- `(17,17)` не попал в итоговую сводку;
- 116,67 вычислительных часа;
- 1 400 985 547 ветвлений;
- 682 897 212 конфликтов.

Вывод: разбиение только по числу насыщенных строк и столбцов слишком грубое. Повтор той же модели только с другими случайными числами неэффективен. CP-SAT не продолжает внутреннее дерево после `UNKNOWN`; сохранённые JSON — статистика, а не контрольная точка.

### 29561750178 — часовая усиленная модель

Ссылка: https://github.com/Grisha-Pochuev/monochromatic-no-three-in-line-solver/actions/runs/29561750178

Архив: `22x22/runs/2026-07-17-run-29561750178/`.

Модель: точные рисунки строк, столбцов и диагоналей `+1/-1`, связанные с переменными точек, плюс все остальные ограничения по прямым.

Технический итог:

- проверка 33-точечной конструкции прошла;
- собраны все 21 из 21 разделов, включая `(17,17)`;
- ошибок модели и пропусков нет;
- `INFEASIBLE`: 0;
- `UNKNOWN`: 21;
- `FEASIBLE/OPTIMAL`: 0;
- 19,45 вычислительных часа;
- 171 178 946 ветвлений;
- 20 825 701 конфликт.

Математический статус не изменился:

```text
33 ≤ D_mono(22) ≤ 34
```

Новая модель не нашла 34 точки, но и не доказала невозможность ни в одном разделе.

Сравнение на один вычислительный час:

- ветвления: примерно 8,80 млн/ч против 12,01 млн/ч у исходной модели (на 27% меньше);
- конфликты: примерно 1,07 млн/ч против 5,85 млн/ч (на 82% меньше).

Это показывает, что кодировка заметно изменила поиск, но не доказывает, что она ближе к завершению. Не запускать её без изменений на шесть часов.

Самые тяжёлые по текущему числу ветвлений:

- `(14,14)`: 30 163 405 ветвлений, 9 422 937 конфликтов;
- `(12,17)`: 18 875 536 ветвлений, 4 879 994 конфликта.

### Новый точный путь — рациональный сертификат и пара на главной диагонали

Для цели 34 найден и отдельно проверен четырёхнаправленный рациональный сертификат:

```text
знаменатель: 187
целевая сумма весов: 6470/187 ≈ 34,598930
запас при 34 точках: 6470 - 34*187 = 112
```

Три диагонали имеют вес больше 112 и потому в любой 34-точечной конфигурации обязаны содержать ровно по две точки:

```text
x-y = -2
x-y = 0
x-y = 2
```

Следовательно, на главной диагонали `x=y` обязательно выбрана ровно одна пара. Всего пар 231; поворот на 180 градусов сокращает их до полного набора из 121 канонического случая.

Подготовлены:

- `22x22/upper_certificate_34_four_direction.json`;
- `22x22/prove_upper_22x22_main_diagonal_pair.py`;
- `22x22/run_n22_main_diagonal_shard.py`;
- `22x22/collect_n22_main_diagonal_results.py`;
- `22x22/MAIN_DIAGONAL_PAIR_PLAN.md`;
- `.github/workflows/n22-main-diagonal-pairs-five-hour.yml`.

Движок содержит все 2455 максимальных прямых, проверяет сертификат, фиксирует конкретную пару на `x=y`, сохраняет `UNKNOWN` как незавершённый результат и независимо перепроверяет найденную 34-точечную конфигурацию.

Короткая локальная проверка пройдена:

- сертификат проверен точно;
- получено ровно 121 каноническое разбиение;
- прямые случаи дают корректные JSON;
- тестовый шард из семи случаев сохраняет все записи;
- сборщик проверен на полном искусственном наборе из 121 записи.

Пятичасовой запуск распределяет 121 случай по 20 заданиям: 19 заданий по 6 случаев и одно задание из 7 случаев. Внутренний бюджет каждого задания — 17 400 секунд, чтобы оставить время на сохранение артефактов.

---

## 6. Как понимать статусы

- `INFEASIBLE`: раздел строго невозможен.
- `FEASIBLE` или `OPTIMAL`: найдена конфигурация цели; её отдельно проверяет независимый скрипт.
- `UNKNOWN`: решатель не успел найти конфигурацию или доказать невозможность. Это не доказательство и не ошибка.
- `MODEL_INVALID`: ошибка модели или параметров; результат непригоден.
- `cancelled`: неполный внешний останов.

Красный крест GitHub Actions сам по себе не является математическим выводом. `UNKNOWN` должен сохраняться в JSON и не превращаться в техническое падение.

---

## 7. Обязательный порядок перед тяжёлым запуском

1. Один раз прочитать этот файл.
2. Проверить очередь Actions, SHA, название workflow и отсутствие дублирующего запуска.
3. Сначала провести короткую проверку: зависимости, все части разбиения, запись JSON, сборка сводки, загрузка артефактов, корректная обработка `UNKNOWN` и `MODEL_INVALID`.
4. Закреплять OR-Tools, сейчас предпочтительно `ortools==9.15.6755`.
5. Всегда приводить seed к 32-битному диапазону и сохранять оба значения.
6. Оставлять запас времени: около 3500 секунд решателю при часовом лимите.
7. Стандартная схема: до 20 заданий, 4 потока CP-SAT на задание, около 14–16 ГБ памяти; процессоры могут различаться.
8. Загружать артефакты с `if: always()` и записывать JSON до выхода.
9. Различать `PROVED`, `FOUND_COUNTEREXAMPLE`, `INCOMPLETE`, `TECHNICAL_FAILURE`.
10. Не закрывать 22×22, пока в полном разбиении остаётся хотя бы один `UNKNOWN`, пропуск или отменённая часть.

---

## 8. Как сохранять значимый прогон

Папка:

```text
NxN/runs/YYYY-MM-DD-run-RUN_ID/
```

Минимум:

- `SUMMARY.md`;
- `overall.json` или `partial_results.json`;
- все записи разделов либо единый файл, содержащий их полностью;
- модель, параметры, ссылка на Actions и SHA запуска;
- ожидаемое и полученное число частей;
- статусы, время, ветвления и конфликты;
- точная рекомендация следующего шага.

После разбора:

1. закоммитить архив;
2. обновить README соответствующего размера;
3. корневой README менять только при изменении подтверждённого статуса;
4. обновить `START_HERE.md`;
5. не подменять старый запуск новым workflow.

---

## 9. Следующая исследовательская линия по 22×22

Текущий точный путь — полное разбиение по выбранной паре на вынужденно насыщенной главной диагонали `x=y`.

Порядок:

1. проверить все 121 канонический случай пятичасовым workflow;
2. если найдено 34 точки — независимо проверить и сохранить конфигурацию;
3. если все 121 случая `INFEASIBLE` — закрыть доску со значением 33;
4. если останутся `UNKNOWN`, повторно запускать только их;
5. каждый оставшийся случай дробить по полному диапазону числа насыщенных диагоналей `x-y=const`, от 13 до 17;
6. при необходимости добавить аналогичный параметр для диагоналей `x+y=const` или точные граничные профили.

Старую усиленную модель без фиксации пары больше не запускать долгим повтором. Любое дальнейшее разбиение должно быть полным: нельзя пропустить допустимые пары или значения диагонального параметра.

---

## 10. Общая линия после 22×22

Не превращать проект только в последовательность дорогих переборов. Параллельно развивать:

- нижние конструкции: CP-SAT, стохастический SAT, локальный поиск, симметричные и почти симметричные шаблоны;
- верхние границы: линейные сертификаты, точные разбиения, дополнительные семейства наклонов;
- закономерности: профили строк, столбцов, диагоналей, симметрии, различия чётных и нечётных размеров.

Формула `ceil(3n/2)` полезна как наблюдение для ряда размеров, но не является доказанной общей границей.

---

## 11. Правила работы в веб-чате

Пользователю сначала объяснять человеческий смысл, потом технические подробности. Не начинать исследование заново, не повторять уже проверенное, не заявлять о закрытии без строгого подтверждения. Тяжёлые расчёты выполнять через GitHub Actions; локально — только короткие проверки. После запуска давать ссылку, после завершения писать, что доказано, что не доказано и что делать дальше. После значимого шага коммитить результаты и обновлять этот файл.

---

## 12. Текущая точка продолжения

Активный размер: `22×22`.

Подтверждённый статус:

```text
33 ≤ D_mono(22) ≤ 34
```

Последний полностью сохранённый запуск: `29561750178`.

Подготовленный следующий workflow:

```text
.github/workflows/n22-main-diagonal-pairs-five-hour.yml
```

Точка продолжения:

```text
Снять результаты полного пятичасового прогона 121 канонической пары главной диагонали. Если остались UNKNOWN, дробить только их по числу насыщенных диагоналей +1 в полном диапазоне 13..17.
```
