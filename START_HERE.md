# START HERE — долговременная память проекта

Последнее существенное обновление: **22 июля 2026 года**, после проверки прогона `29918490062`.

Этот файл нужно читать в начале каждого нового чата проекта. При расхождении старого чата с проверяемыми файлами доверять репозиторию.

## Задача

На доске `n × n` выбираются точки одного шахматного цвета. Никакие три выбранные точки не должны лежать на одной прямой любого наклона. Максимум обозначается `D_mono(n)`.

Проверяются **все прямые**, а не только строки, столбцы и диагонали.

## Структура репозитория

Каждый размер обязательно имеет отдельную папку непосредственно в корне:

```text
17x17/
18x18/
19x19/
20x20/
21x21/
22x22/
23x23/
...
```

Результаты разных досок нельзя смешивать. Каждый значимый запуск хранится внутри папки своей доски:

```text
NxN/runs/YYYY-MM-DD-run-RUN_ID/
```

Workflow-файлы находятся в `.github/workflows/`.

## Подтверждённые результаты

| Размер | Результат | Статус |
|---:|---:|---|
| `17×17` | `D_mono(17)=26` | закрыт |
| `18×18` | `D_mono(18)=27` | закрыт |
| `19×19` | `D_mono(19)=29` | закрыт |
| `20×20` | `D_mono(20)=30` | закрыт |
| `21×21` | `D_mono(21)=32` | закрыт |
| `22×22` | `33 ≤ D_mono(22) ≤ 34` | активный фронтир |

Размер закрыт только при проверенной нижней конструкции и строгом верхнем доказательстве. `UNKNOWN` не является доказательством.

## Активная доска 22×22

Проверенная конструкция из 33 точек находится в `22x22/config_33.json`:

```bash
python 22x22/verify_22x22.py
```

Она доказывает `D_mono(22) ≥ 33`. Верхний поиск проверяет возможность 34 точек.

Полное исходное точное разбиение содержит 2640 дочерних случаев. Уже доказанный `INFEASIBLE` никогда не пересчитывать.

## Основная цепочка прогонов 22×22

```text
29766054707: 1864 INFEASIBLE, 776 UNKNOWN
29770927206: ещё 606 INFEASIBLE, осталось 170
29795643015: ещё 114 INFEASIBLE, осталось 56
29815441947: ещё 46 INFEASIBLE, осталось 10
29842033980: ещё 8 INFEASIBLE, осталось 2
29868888706: закрыт ещё 1 случай, остался child 652
29918490062: child 652 разделён на 21 часть; 4 INFEASIBLE, 17 UNKNOWN
```

Архивы лежат только в `22x22/runs/`.

## Проверенный итог прогона 29918490062

Ссылка:

```text
https://github.com/Grisha-Pochuev/monochromatic-no-three-in-line-solver/actions/runs/29918490062
```

Архив:

```text
22x22/runs/2026-07-22-run-29918490062/
```

Единственный оставшийся ребёнок `652` был разделён на 21 непересекающийся случай:

```text
12 ≤ row_twos ≤ column_twos ≤ 17
```

Итог:

```text
точных записей: 21 / 21
INFEASIBLE: 4
UNKNOWN: 17
FEASIBLE/OPTIMAL: 0
MODEL_INVALID: 0
пропуски/дубли/неожиданные записи: 0
работа: 39,09 вычислительного часа
ветвления: 821 921 325
конфликты: 502 969 578
```

Строго закрыты:

```text
subcase 5  = (row_twos=12, column_twos=17)
subcase 10 = (row_twos=13, column_twos=17)
subcase 17 = (row_twos=15, column_twos=17)
subcase 19 = (row_twos=16, column_twos=17)
```

Остались:

```text
0, 1, 2, 3, 4, 6, 7, 8, 9, 11, 12, 13, 14, 15, 16, 18, 20
```

Ни одной 34-точечной конфигурации не найдено. Статус доски остаётся:

```text
33 ≤ D_mono(22) ≤ 34
```

Наблюдение: **все четыре** строгих исключения имеют `column_twos=17`. Край с максимальным числом насыщенных столбцов оказался легче, но это пока не общая лемма.

## Правила тяжёлых запусков

1. Сначала прочитать этот файл.
2. Проверить очередь Actions, SHA и отсутствие дубликата.
3. Перед запуском проверить движок, расписание, JSON, сборщик и артефакты.
4. Использовать `ortools==9.15.6755` во всех заданиях.
5. Seed приводить к знаковому 32-битному диапазону и сохранять оба значения.
6. Использовать до 20 параллельных заданий и 4 потока CP-SAT на задание.
7. Не запускать несколько решателей внутри одной 16-ГБ машины без осознанного деления потоков.
8. Всегда загружать результаты через `if: always()`.
9. Различать математический `UNKNOWN`, техническую ошибку и memory guard.
10. Не закрывать доску, пока остаётся хотя бы один `UNKNOWN` или пропуск.
11. После запуска сохранять всё в папке соответствующего размера и обновлять README и этот файл.
12. Следующий прогон строить только по текущим `UNKNOWN`.

## Точка продолжения

Активный размер: `22×22`.

Обрабатывать только 17 оставшихся подслучаев ребёнка `652`:

```text
0, 1, 2, 3, 4, 6, 7, 8, 9, 11, 12, 13, 14, 15, 16, 18, 20
```

Четыре закрытых подслучая и 2639 более ранних детей не пересчитывать. Если все 17 станут `INFEASIBLE`, вместе с конструкцией из 33 точек получаем `D_mono(22)=33`. Если найдена конфигурация из 34 точек, её нужно независимо проверить, после чего получаем `D_mono(22)=34`.
