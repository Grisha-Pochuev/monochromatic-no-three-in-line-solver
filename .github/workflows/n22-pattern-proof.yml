name: n22 stronger pattern exact proof

on:
  push:
    paths:
      - '22x22/RUN_N22_PATTERN_PROOF'
  workflow_dispatch:

concurrency:
  group: n22-pattern-proof
  cancel-in-progress: true

jobs:
  superseded:
    name: Superseded by one-hour pattern smoke
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "The queued six-hour run was superseded by the robust one-hour pattern smoke."
          echo "The full workflow definition is restored in the following commit."
